# This is an example file showing required environment variables
# Copy this to .env.local and fill in the actual values for development
# For production, configure these in your Vercel dashboard

# Database connection string
# Development: SQLite file path
# Production: PostgreSQL connection string from Vercel Postgres or similar service
DATABASE_URL="postgresql://user:password@host:5432/database"

# Authentication secret - must be a strong random string
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# Application URL - used for session management
# Development: http://localhost:3000
# Production: https://fidelite.delta-fraternite.com
NEXTAUTH_URL="https://fidelite.delta-fraternite.com"
